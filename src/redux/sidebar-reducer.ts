let initialState = {}

type SidebarReducerType = {
    type: typeof initialState
}

const sidebarReducer = (state: SidebarReducerType, action: any): SidebarReducerType => {
    return state;
}

export default sidebarReducer;