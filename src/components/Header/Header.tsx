import React from 'react';
import styled from "./Header.module.css"

const Header = () => {
    return (
        <div className={styled.wrapperH}>
          
        </div>
    );
}
export default Header